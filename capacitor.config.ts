import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mi-buseta-app',
  webDir: 'www',
  server: {
    cleartext: true,
  },
};

export default config;
